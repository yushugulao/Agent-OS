# Next Work For Plain uCore Migration

The current branch has a working native baseline. It now includes active cross-file consistency checks, concrete RUN-042 artifact files, three custom research tasks stored in ordinary files, nine small CSV-style rows, request-form, uploaded-material, reusable-source, workspace-import, generated workspace-template, workspace-run sections, four dynamic submission records, file-backed validation state, and host UI feed hints inside `rp_input`, three derived custom run summaries and one queued API run inside `rp_runner`, research workbench task state appended to `rp_runner`, workbench readiness, cited-answer, handoff brief, continuation runbook, timeline, and file-manifest sections inside `rp_runner`, human review records, requested-change records, revision-task records, one revised run record, applied revision deltas, review thread records, review comment records, action item records, bibliography and citation-plan records, literature search, candidate screening, evidence extraction, registered evidence protocol, PRISMA-style flow, evidence synthesis, data ingestion records, normalized FASTQ, alignment table, metrics JSON, gene-count CSV, archive manifest sections, artifact dossier links, artifact provenance records, artifact review path records, and artifact path rebuild checks inside `rp_artifact` / `rp_artifact_manifest` / `rp_consistency`, dataset snapshots, data previews, quality results, transform records, dataset collection export, workflow runner execution files with command/output records, dependency checks, retry reasons, cache policy, support links, dossier checks, reader-derived workflow execution, control, and evidence link rows from stage/cache/retry/event/worker/observer/artifact/report/review records, workflow portability records for Snakemake, Galaxy, DVC, CWL, and Nextflow style workflows, adapter summaries, migration plans, rehearsal cases, workflow portability delivery checks, workflow portability to backend execution checks, backend runner case checks, backend runner detail checks with content checks, attempts, retry reasons, tick observations, source/requirement/observation/action/review rows, plain-cost/AgentOS-replacement/risk rows, and backend evidence links into `rp_runner`, `rp_report_text`, and review handoff state, object naming checks, surface reachability checks, status semantics checks, reference checks, evidence trace checks, run-state explanation checks, lifecycle-order checks, delivery consistency checks, AgentOS readiness checks, static review site inventory with 42 routed pages, Agent collaboration records, file-backed Host LLM Relay protocol files with queue validation, route decisions, packet schema checks, guard checks, fallback decisions, and matched request, packet, and response records, Host LLM Relay request/response/fallback actions that carry request id, route, provider, response id, summary, and fallback case into `rp_llm_*`, `rp_api_runtime`, `rp_actionio`, and `rp_web_bundle`, host relay quality records in `rp_llmeval`, packet guard records in `rp_llm_guard`, replay records in `rp_relay`, prompt route records in `rp_prompt`, a dedicated LLM Relay reader page with relay flow and LLM action trace/output/impact/delta tables, native review dashboard state in `rp_review_dashboard`, a dedicated `rp_runbooks` service for incident triage, seven recovery steps, execution export, worker handoff, and AgentOS replacement targets, a dedicated `rp_projectrel` service for project handoff audit, release gate, snapshots, reproducibility audit, provenance graph, delivery report, package intake, and AgentOS replacement targets, reviewer evidence package state materialized as `rp_review_pack` by the host relay with delivery, operations, project-space, workbench handoff, backend evidence links, backend action/review rows, operations/workbench/project summaries, operations report narrative inputs, operations source-file links, and related Host action trace, bio service files, lab resource service files, publication service files, knowledge service files, runtime service files, startup guide and platform doctor service state, a Host Reader services page that renders operation records and runbook service records from those files, a Host Reader data page that renders ingest, snapshot, preview, quality, transform, collection, and manifest verification records from existing files, a Host Reader project page that renders project-space, evidence-package, quality, repair, search, and source-file records from existing files, a Host Reader delivery page that renders package files, notebook export, usable-run, review-pack delivery, and source-line records from existing files, reproducible notebook export records, package export indexes for report, evidence, provenance, reusable source selection, reviewer delivery, delivery file rows, delivery checks, delivery manifest JSON/Markdown names, evidence bundle zip contents, raw artifacts, and review page sections inside `rp_package`, host UI export files with navigation, timeline, artifact preview, Agent decision, evidence preview, dynamic input queue references, live-update feed records, host reader contract records, notebook download reference, workflow execution view tables, workflow control view tables, workflow evidence link tables, platform-written report source map tables with source keys resolved to actual state lines, artifact source map tables with references resolved to actual state lines, review source map tables with handoff references resolved to actual state lines, delivery source map tables with package references resolved to actual state lines, action-output link and detail tables, action-impact tables for report/artifact/review/LLM targets, action-delta tables for request-to-output field comparison, Run/Workflow/Workbench/Project/Data/Compare/Review/Delivery/LLM Host action trace tables, operations report narrative tables, operations source-file tables, artifact review path tables, comparison metric rows, backend runner case tables, backend case detail tables, backend evidence report tables, derived backend case narrative tables, backend study metric tables, and backend scenario handoff status, Host Web/API payload files, file-backed POST action request and response records, twelve service action records with validation, precondition checks, idempotency keys, side-effect records, download-manifest state, knowledge-index records, provenance graph counts, event stream counts, LLM transcript scale records, workbench delivery/project-operation scale records, research portfolio scale records, execution scale records, operations scale records, project scaffold/revision/incident records, reserved empty research object categories, root project/run/report/plan/search/site/profile/audit/context records, host state category catalog, AgentOS adapter and ABI reserved object categories, and a 1070-check user-space test suite over ordinary uCore state files. The remaining work is to keep moving behavior from compact records into active user-space services without changing the kernel.

## Required Direction

The plain uCore platform should keep the same object names and output contract as the Python platform and the future Agent-OS platform. New work should add behavior in user space, not kernel hooks.

The host-side platform is being upgraded from metadata-heavy records to a real research Agent application. The plain uCore migration must track that higher target. It may expose limitations of an unchanged kernel, but it should not remove the same workflow concepts.

## Host Parity Targets

The plain uCore version should preserve these host-platform capabilities in a form that can run on an unchanged uCore kernel:

- Web UI data: uCore does not need to serve HTTP directly, but it should produce ordinary files for a host UI home page, run detail view, Agent detail view, evidence detail view, project view, data view, artifact view, comparison metrics view, bio view, lab resource view, publication view, knowledge view, runtime view, route manifest, API payloads, artifact preview entries, POST action records, request validation records, and side-effect records.
- Real artifact operations: read ordinary input files, write intermediate artifacts, report text, logs, and chart data instead of only writing summary metadata.
- Data pipeline: preserve input scanning, dataset snapshots, previews, quality checks, transformations, and dataset collection output as ordinary files that the host UI and future Agent-OS version can consume.
- Minimal workflow runner: represent a stage DAG, dependencies, failure, retry, cache reuse, and per-stage logs as ordinary files.
- Host LLM relay: keep a request queue, route table, packet schema checks, guard checks, fallback decisions, response files, quality records, and a reader page that support template mode without a key and live cloud mode on the host when a key exists; no secret should be stored inside the uCore image.
- Agent collaboration: keep orchestrator, retriever, analyst, reviewer, writer, recovery, and auditor messages plus decision records as executable user-space artifacts.
- AgentCompare: preserve explicit plain-kernel pain points: many file scans, convention-based state, user-space-only permissions, untrusted context, expensive path reconstruction, and multi-step failure recovery.
- Test growth: keep increasing small tests or check programs around core services, exported UI data, Host Web/API payload files, host reader contract fields, file-backed action records, action validation records, action side-effect records, demo run artifacts, custom research input fields, dynamic input queue fields, host UI feed fields, workbench task state, workbench readiness, cited answer, handoff brief, continuation runbook, timeline, file manifest, notebook export records, request-form sections, uploaded-material sections, reusable-source sections, workspace-import records, bibliography records, citation-plan records, literature search records, screening decisions, evidence extraction records, evidence protocols, PRISMA-style flow records, workflow portability records, workflow portability delivery checks, workflow portability to backend execution checks, backend runner case checks, backend runner detail checks, backend case detail rows, backend evidence report rows, backend evidence report links, adapter summaries, migration plans, rehearsal cases, object naming checks, surface reachability checks, status semantics checks, reference checks, evidence trace checks, run-state explanation checks, lifecycle-order checks, delivery consistency checks, AgentOS readiness checks, static review site pages, delivery manifests, delivery file rows, delivery checks, evidence bundle contents, review pages, review dashboard, review handoff checks, runbook recovery service records, project delivery and reproducibility service records, reviewer evidence package, backend evidence handoff, human review records, requested-change records, applied revision changes, revision-task records, review thread records, review comment records, action item records, active action records, data pipeline files, derived FASTQ/alignment/metrics/count/archive sections, artifact dossier rows, artifact provenance records, artifact path rebuild checks, dossier checks, package export indexes, Host UI render data, service surface records, startup guide and platform doctor records, workflow runner execution files, workflow runner detail fields, Agent collaboration records, LLM relay queue validation, LLM delivery checks, route decisions, packet schema checks, guard checks, fallback decisions, request/packet/response records, knowledge-index records, LLM transcript scale records, workbench delivery/project-operation records, research portfolio records, execution scale records, operations scale records, project scaffold/revision/incident records, reserved empty research object categories, root project/run/report/plan/search/site/profile/audit/context records, host state category catalog, AgentOS adapter and ABI reserved object categories, and AgentCompare records. The current uCore branch already runs a 1070-check user-space suite.
- Real run evidence: every major output should be tied to a concrete input, generated artifact, observed failure, recovery action, report section, or comparison result.

## Planned Increments

1. Expand the current reusable object collections into larger workflow, workflow invocation, workflow completion, run configuration, backend scenario, evidence, claim-record, provenance-path, knowledge, dataset, data-profile, figure, report, project-policy, compliance, risk, CAPA, release-delta, execution-plan, execution-observer, FAIR data release, data product version, reproduction, LLM packet queue, host relay, LLM evaluation, prompt routing, sample, protocol, experiment, trial, telemetry, health, and review records.
2. Expand catalog, query, object query, lineage, site export, evidence, claim-record, provenance-path, knowledge, package, data release, data version, release delta, reproduction, review governance, release, dossier, submission, compare, planner, retriever, analyst, data dictionary, data profile, figure record, calculation replay, sample, quality, protocol, SOP, experiment, trial record, lab operations, telemetry, health, reviewer, governance, writer, repair, auditor, scheduling, task ranking, workflow import/export, resource budget, project policy, risk register, CAPA records, run configuration, workflow invocation, workflow completion, execution observer, backend scenario, failure classification, run views, retry handling, LLM packet queue, host relay, LLM evaluation, prompt routing, privacy, compliance, and AgentCompare programs beyond the current fixed records.
3. Deepen the current file-backed task message, acknowledgement, scheduling, task-record, task-ranking, workflow import/export, run configuration, workflow invocation, workflow completion, execution plan, worker health, run timeline, budget, policy, compliance, risk, CAPA, release-delta, failure, retry, run-view, review-round, revision, data-version, and tool-log protocol with richer scheduling and review cases.
4. Continue moving selected checks from static counters into active operations over stored records. The current consistency checker already validates task, LLM, workflow invocation, completion, and backend scenario relations.
5. Add an AgentCompare plain-kernel runner that emits the same high-level result fields as the future Agent-OS runner.
6. Expand the host-side relay process with more provider adapters, stricter response scoring, and route-specific validation over the prepared ordinary LLM request queue.
7. Continue expanding uCore user-space exports for UI pages and run details so the host web service can render the same scenario from plain-kernel output files. The current branch already exports home, run, Agent, evidence, project, data, comparison, bio, lab resource, publication, knowledge, and runtime page data, route manifest, API payload files, and file-backed action records for host workflow, AgentCompare, custom research execution, dynamic research submission, human review, revision-task creation, and revised-run execution. The custom research requests, CSV-style rows, dynamic submission rows, derived run summaries, human review records, revision-task records, review thread records, review comment records, and action item records are carried in existing files to avoid exhausting the compact uCore root file system.
8. Continue expanding concrete input, artifact, report, log, chart-data, data-pipeline, and per-stage runner records for `RUN-042`. The current branch already writes a small FASTQ input, input scan records, dataset snapshots, preview records, quality results, transform records, dataset collection, stage DAG, stage log, stage-state table, cache index, retry plan, event stream, artifact manifest, recovered align artifact, report text, chart data, and runner summary.

## Non-Goals For This Branch

- No Agent syscall implementation.
- No kernel Agent Context.
- No kernel file metadata service.
- No kernel Agent event queue.
- No kernel LLM networking.

Those belong to the enhanced kernel version.
