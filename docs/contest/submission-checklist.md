# 竞赛提交检查清单

本清单依据仓库外的《Agent-OS 赛题说明》以及当前仓库状态整理。勾选项表示材料已经存在，
不表示当前提交已经通过动态验收。最终提交负责人应在冻结提交上逐项复核并记录签名与日期。

## 已具备

- [x] RISC-V 64 uCore 内核源码和 AgentOS 内核模块位于 `os/`、`nfs/`。
- [x] 任务一至五的用户态测试和任务六综合示例位于 `user/`。
- [x] 根目录 AgentOS 目标与 `baseline_ucore/` 对照目标使用同一类科研 Agent 负载。
- [x] 提供 QEMU 构建、运行、双目标、专项测试和完整验收入口。
- [x] 提供架构、ABI、任务说明、运行方法、测试方法与要求追踪文档。
- [x] 提供源代码 GPL-3.0、文档 CC BY-SA 4.0 许可文本和现有第三方通知。
- [x] 提供演示视频和幻灯片的外部下载入口。
- [x] 评价框架拒绝用公式数据、硬编码成功标记或无原始日志的摘要冒充正式证据。

## 发布阻塞项

- [ ] 在固定执行环境中核对最终提交的受管测试源码指纹；与有效校准指纹一致时复用，发生漂移时在干净 detached worktree 重新完成三轮完整 18-case 校准并提交记录。
- [ ] 用校准后的策略从干净 worktree 完整通过 `make full-verify`。
- [ ] 完成正式评价 campaign，而不是 development/smoke 运行；保存全部原始 Guest/Host 工件。
- [ ] 生成 Dashboard，并从原始数据重放验证，确认性能结论是 `supported`、`regressed` 或
  `inconclusive` 中的真实结果。
- [ ] 按 C（代码）到 E（证据）的提交合同生成、提交并复验正式 release bundle。
- [ ] 向 `evidence/releases/INDEX.md` 追加由采集器生成的正式记录；不得手工伪造索引行。
- [ ] 在干净 clone 中复验 bundle、Git 历史、文件哈希和全部离线 Dashboard 数据。
- [ ] 对无法执行的远程 CI 如实标记 `not-attached`，不得写成 CI 通过。

## 答辩材料

- [ ] 冻结最终答辩 PPT/PDF、演示视频和演示脚本，材料内容与最终提交一致。
- [ ] 使用[材料清单模板](materials-manifest-template.md)记录提交 C、证据提交 E、tag、文件大小和
  SHA-256；外链材料也必须记录其实际字节哈希。
- [ ] 在另一台机器或干净环境演练 2 至 3 分钟快速演示和完整演示。
- [ ] 演示开始先说明 `baseline_ucore/` 是共享安全加固对照，不是原始上游 uCore。
- [ ] 只展示正式 bundle 支持的性能结论；`inconclusive` 结果也应如实呈现。
- [ ] 准备无网络、无云模型密钥时的默认离线 LLM Relay 路径。
- [ ] 检查网盘链接、提取码、下载权限和长期有效性，并准备离线备份。

## 合规与署名

- [ ] 赛队负责人确认[AI 工具使用披露](ai-usage-disclosure.md)覆盖所有实际使用的模型和工具。
- [ ] 赛队负责人确认[第三方与原创增量说明](third-party-and-originality.md)与 Git 历史一致。
- [x] 三个 `.clang-format` 文件的 Linux 内核来源和独立 `GPL-2.0` 许可已写入 `NOTICE`，
  原 SPDX 标记保持不变。
- [ ] 检查仓库未包含 GitLab token、LLM API key、个人路径、临时日志或未授权数据。
- [ ] 确认 PPT、视频、图片、字体、样例数据和引用材料均可提交并已正确署名。
- [ ] 创建只指向冻结提交的最终 tag，并在提交平台核对默认分支、commit ID 和材料版本。

## 最终签收

| 项目 | 记录 |
| --- | --- |
| 最终源码提交 C | `待填写` |
| 最终证据提交 E | `待填写` |
| 最终 tag | `待填写` |
| release bundle | `待填写` |
| `full-verify` 状态 | `待填写，不得引用历史提交` |
| 正式评价状态 | `待填写` |
| 复现人/日期 | `待填写` |
| 提交负责人/日期 | `待填写` |
